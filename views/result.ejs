<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Candidate Result</title>
  <link rel="stylesheet" href="/public/css/main.css">
</head>
<body>
  <header class="header">
    <div class="topbar">
      <div class="brand"><img src="/public/img/logo.svg" alt=""><div class="title">CAREERS</div></div>
      <a class="contact-btn" href="#">CONTACT US</a>
    </div>
  </header>

  <div class="wrapper">
    <h1 class="title" style="font-size:28px;margin-bottom:8px">Marks Secured by the Candidate</h1>
    <p class="lead">Details of candidate and marks</p>

    <table class="table table-striped">
      <tr><th style="width:22%">Name :</th><td><%= cand.name %></td><th style="width:22%">Category :</th><td><%= cand.category %></td></tr>
      <tr><th>Post :</th><td><%= cand.post %></td><th>Applied under :</th><td><%= cand.appliedUnder %></td></tr>
      <tr><th>State applied for :</th><td><%= cand.state %></td><th>Date of Birth :</th><td><%= cand.dob %></td></tr>
      <tr><th>Roll No. :</th><td><%= cand.rollNo %></td><th>Reg No. :</th><td><%= cand.regNo %></td></tr>
    </table>

    <table class="table" style="margin-top:18px">
      <tr><th>Total Marks</th><th>Marks Scored</th></tr>
      <tr><td><%= cand.totalMarks %></td><td><%= cand.marksScored %></td></tr>
    </table>

    <h3 style="margin-top:18px;color:var(--purple)">Category Wise Cut-off Marks</h3>
    <table class="table table-striped">
      <tr>
        <% Object.keys(cand.cutoffs).forEach(k => { %>
          <th><%= k %></th>
        <% }) %>
      </tr>
      <tr>
        <% Object.values(cand.cutoffs).forEach(v => { %>
          <td><%= v %></td>
        <% }) %>
      </tr>
    </table>

    <p style="margin-top:20px;font-weight:800;color:#0a7">CONGRATULATIONS — You have been PROVISIONALLY SELECTED subject to qualifying in the Language Test as per Advertisement.</p>
  </div>
  <div class="footer">© Careers Portal.</div>
</body>
</html>
